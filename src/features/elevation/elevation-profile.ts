/**
 * Elevation profile along rover traverse path.
 * TODO(M4): Implement elevation sampling, chart rendering, click-to-seek
 */

/**
 * Initialize the elevation profile for the active rover.
 */
export async function initElevationProfile(): Promise<void> {
  // TODO(M4): Sample elevation via view.ground.queryElevation()
  // TODO(M4): Render line chart
  // TODO(M4): Mark current sol position
  // TODO(M4): Click on chart → seek to that sol
}
