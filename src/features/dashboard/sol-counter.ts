/**
 * Sol calculator widget — converts between Earth dates and Mars sols.
 * TODO(M3): Implement interactive converter UI
 */

/**
 * Initialize the sol-counter UI component.
 */
export function initSolCounter(): void {
  // TODO(M3): Create date input + sol output converter
  // TODO(M3): Wire to solToEarthDate / earthDateToSol utilities
}
