/**
 * Mission dashboard panel — displays current sol stats, mission summary.
 * TODO(M3): Implement dashboard population, rover selector, sol-change listener
 */

/**
 * Initialize the dashboard panel and begin listening for sol-change events.
 */
export function initDashboard(): void {
  // TODO(M3): Query Calcite panel elements
  // TODO(M3): Subscribe to sol-change CustomEvent on document
  // TODO(M3): Populate initial stats for default rover
}
