/**
 * Curiosity REMS weather display widget.
 * TODO(M3): Implement weather fetch, caching, and display
 */

/**
 * Initialize the weather widget — fetches latest REMS data and renders.
 */
export async function initWeatherWidget(): Promise<void> {
  // TODO(M3): Fetch REMS weather JSON
  // TODO(M3): Display latest sol weather in Calcite block
  // TODO(M3): Cache for 1 hour
}
