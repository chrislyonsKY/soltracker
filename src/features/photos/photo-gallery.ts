/**
 * Photo gallery — thumbnail grid synced to sol/position.
 * TODO(M3): Implement gallery rendering, sol-change subscription, lightbox
 */

/**
 * Initialize the photo gallery panel.
 */
export function initPhotoGallery(): void {
  // TODO(M3): Subscribe to sol-change events (debounced 500ms)
  // TODO(M3): Render thumbnail grid in Calcite panel
  // TODO(M3): Click → Calcite dialog with full-res image + metadata
}
