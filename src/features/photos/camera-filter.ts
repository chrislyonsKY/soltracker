/**
 * Camera selection dropdown for photo filtering.
 * TODO(M3): Implement camera dropdown, trigger re-fetch on selection
 */

/**
 * Initialize the camera filter dropdown for a given rover.
 */
export function initCameraFilter(): void {
  // TODO(M3): Populate dropdown from rover camera list
  // TODO(M3): On change, re-fetch photos with camera parameter
}
